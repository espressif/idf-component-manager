Compote CLI
===========

The ``compote`` is a command-line tool for the IDF Component Manager. It works with ESP component registry, ESP-IDF projects and components without requiring ESP-IDF installation.

.. click:: en.conf:compote_cli
    :prog: compote
    :nested: full
